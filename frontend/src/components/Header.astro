<header class="site-header">
  <div class="site-header__brand-nav">
    <a class="brand" href="/">DMH</a>
    <nav class="site-nav" aria-label="Navegación principal">
      <a class="site-nav__link active" href="/categoria/catalogo">Catálogo</a>
      <a class="site-nav__link" href="/categoria/novedades">Novedades</a>
      <a class="site-nav__link" href="/categoria/accesorios">Accesorios</a>
      <a class="site-nav__link" href="/categoria/ofertas">Ofertas</a>
    </nav>
  </div>

  <div class="site-header__actions">
    <button class="icon-button" type="button" aria-label="Buscar">🔍</button>
    <button class="icon-button" type="button" aria-label="Favoritos">♡</button>
    <button class="icon-button" type="button" aria-label="Cuenta">👤</button>
    <a class="cart-button" href="/carrito" aria-label="Carrito de compras">
      🛒
      <span class="cart-badge">2</span>
    </a>
  </div>

  <style>
    .site-header {
      width: 100%;
      max-width: 1200px;
      margin: 0 auto;
      padding: 1.25rem 2rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
      color: #f5f5f5;
    }

    .brand {
      font-family: Inter, system-ui, sans-serif;
      font-size: 1.4rem;
      font-weight: 700;
      letter-spacing: 0.24em;
      color: #d7ff00;
      text-transform: uppercase;
      margin-right: 2rem;
    }

    .site-nav {
      display: flex;
      gap: 1.6rem;
      flex-wrap: wrap;
      align-items: center;
    }

    .site-nav__link {
      color: rgba(245, 245, 245, 0.8);
      font-size: 0.95rem;
      text-decoration: none;
      transition: color 0.2s ease;
    }

    .site-nav__link:hover,
    .site-nav__link.active {
      color: #ffffff;
    }

    .site-header__brand-nav {
      display: flex;
      align-items: center;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .site-header__actions {
      display: flex;
      align-items: center;
      gap: 0.85rem;
      flex-wrap: wrap;
    }

    .icon-button,
    .cart-button {
      border: none;
      background: rgba(255, 255, 255, 0.04);
      color: #f5f5f5;
      width: 42px;
      height: 42px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 999px;
      cursor: pointer;
      transition: background 0.2s ease, transform 0.2s ease;
      text-decoration: none;
    }

    .icon-button:hover,
    .cart-button:hover {
      background: rgba(255, 255, 255, 0.12);
      transform: translateY(-1px);
    }

    .cart-button {
      position: relative;
    }

    .cart-badge {
      position: absolute;
      top: -6px;
      right: -6px;
      min-width: 18px;
      height: 18px;
      border-radius: 999px;
      background: #d7ff00;
      color: #090909;
      font-size: 0.75rem;
      font-weight: 700;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0 0.35rem;
    }

    @media (max-width: 900px) {
      .site-header {
        flex-direction: column;
        align-items: flex-start;
        padding: 1rem 1.5rem;
      }

      .site-header__actions {
        width: 100%;
        justify-content: flex-start;
      }
    }

    @media (max-width: 640px) {
      .site-header {
        padding: 0.9rem 1rem;
      }

      .site-nav {
        gap: 1rem;
      }
    }
  </style>
